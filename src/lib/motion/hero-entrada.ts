/** Splash hero entrance — shared between HeroTitulo and overlay Cabecalho. */

/** Splash words settle at ~500ms — snappier than the loader's tagline. */
export const HERO_REVELA_MS = 500;

/** Seconds before OISHI line (and overlay header) begin — after loader fade. */
export const OISHI_ATRASO = 1;

export const OISHI_TEXTO = "OISHI";

/** 味 overlay + hanko seal — same beat as the S in OISHI (index 2). */
export const SELO_INDICE = 2;
